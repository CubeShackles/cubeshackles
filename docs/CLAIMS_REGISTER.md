# Claims Register

**Owner: CubeShackles (founder-led).**

Tracks material public claims that need explicit evidence, a maturity label,
or founder/legal sign-off before (re)publication in either language. See
[`DOCUMENTATION_STANDARD.md`](DOCUMENTATION_STANDARD.md) §3.

Columns: **Claim** · **Repository** · **Source evidence** · **Maturity** ·
**Verification status** · **Approved EN wording** · **Approved PT wording** ·
**Reviewer** · **Last reviewed**.

| Claim | Repository | Source evidence | Maturity | Verification status | Approved EN wording | Approved PT wording | Reviewer | Last reviewed |
|---|---|---|---|---|---|---|---|---|
| KYC/KYB/KYT and sanctions screening | `cubeshackles-compliance-engine` | README already states "stubs"; boolean-flag evaluation, no live provider integration | Prototype | Not independently verified beyond repo's own README | "Deterministic sandbox compliance control logic. Does not represent legal authorization, regulatory approval, or live AML/KYC integration." (already in repo) | Direct translation, unchanged scope | — | 2026-07-18 (register created) |
| BNA/CMC/BODIVA regulatory connectivity | `cubeshackles-regulatory-reporting` | Repo reformats local JSON only; no outbound connection found | Prototype | Needs founder confirmation before any bilingual doc implies real regulator integration | "Designed to support regulatory reporting integrations; no live connection to BNA, CMC, or BODIVA exists in this repository." | Direct translation | — | 2026-07-18 |
| USSD/telecom bridge | `Cubeshackles-phone-wedge` | Audit found only a browser JS simulator, no real telecom bridge | Prototype | Needs founder confirmation | "This repository provides a browser-based simulator of USSD/telecom access, not a live telecom integration." | Direct translation | — | 2026-07-18 |
| Citizen balance data | `national-transit-app-cubeshackles` | `get_balance()` returns a hardcoded value for any user (per prior audit) | Prototype/demo | Needs founder confirmation before publishing any "live balance" language | "Balance display in this repository is demonstration data, not a live account query." | Direct translation | — | 2026-07-18 |
| Custody signing (HSM/KMS) | `cubeshackles-vault` | Production adapters raise `ProductionSignerNotConfiguredError`; sandbox signer is real and tested | Split: sandbox = implemented, production = planned | Verified in repo tests (sandbox); production path not built | "Sandbox signing is implemented and tested. Production HSM/KMS signing is not yet configured — see `ProductionSignerNotConfiguredError`." | Direct translation, keep the split explicit | — | 2026-07-18 |
| Validator consensus | `Cubeshackles-validator-node` | No raft/pbft/gossip implementation found; "quorum" is a dashboard field | Prototype | Needs founder confirmation | "This repository does not yet implement a live consensus protocol; validator coordination fields are dashboard/reporting surfaces." | Direct translation | — | 2026-07-18 |
| Disaster-recovery drill evidence | `cubeshackles-disaster-recovery` | Prior audit found placeholder-looking hash values and empty RPO/RTO fields on the one recorded drill | Unverified | Do not cite the existing drill as evidence of DR capability in bilingual docs until re-run for real | "A disaster-recovery drill has been recorded but its evidence has not been independently verified; treat current DR capability as unproven." | Direct translation | — | 2026-07-18 |
| Provincial coverage | `cubeshackles-provincial-topology` | Only 5 of 18 Angola provinces modeled; `deploy_live_failover` raises `NotImplementedError` | Prototype | Verified against repo state at audit time; re-check before citing a coverage number | "Provincial topology currently models a subset of Angola's provinces; live failover deployment is not yet implemented." | Direct translation, keep the "subset" qualifier — do not state a specific count without re-verifying against current code | — | 2026-07-18 |

| CIEL signature verification on ledger calls | `cubeshackles-ledger` | `app/core/ciel.py` checks `X-CIEL-Signature` for non-empty presence only; no cryptographic verification call found anywhere in `app/` (verified 2026-07-19) | Planned | Verified against repo code | "Trust boundary" section states verification is presence-only, not cryptographic, explicitly | Direct translation, keep the distinction explicit | — | 2026-07-19 |
| Journal DAG / Merkle root adapter | `cubeshackles-ledger` | `app/ledger/dag/journal_dag_adapter.py` is fully unit-tested (10 tests) but not called by any router or service function (verified 2026-07-19) | Prototype | Verified against repo code | "Architecture" and "Maturity classification" sections state it does not yet contribute to the live journal lifecycle | Direct translation | — | 2026-07-19 |
| Net-position zero-sum invariant in clearing | `cubeshackles-clearing-house` | `services/clearing_service.py` computes and asserts sum of net amounts equals zero before persisting; enforced by `tests/test_clearing_net_positions.py` and a restart-persistence test (verified 2026-07-19) | Implemented | Verified against repo code and passing tests | State as implemented and enforced, cite the invariant explicitly | Direct translation | — | 2026-07-19 |
| No-direct-ledger-mutation boundary in clearing | `cubeshackles-clearing-house` | `tests/test_no_direct_ledger_mutation.py` statically scans for forbidden ledger-mutation call patterns and imports; passing (verified 2026-07-19) | Implemented | Verified against repo code and passing tests | State as implemented and enforced by a dedicated static test, not just a doctrine statement | Direct translation | — | 2026-07-19 |
| Compliance-gate enforcement at clearing intake | `cubeshackles-clearing-house` | `interoperability/gateway_intake.py::validate_gateway_trade_intake` requires compliance-verification fields, but is not called from `process_batch` or any live route — unit-tested in isolation only (verified 2026-07-19) | Prototype | Verified against repo code | State explicitly that compliance-hash validation exists as a library function but is not yet wired into the live `/v0.1/clearing/batches` path | Direct translation, keep "not yet wired" explicit | — | 2026-07-19 |
| Settlement-engine ledger posting call | `cubeshackles-settlement-engine` | `ledger/posting_client.py` performs a real in-process double-entry post against `cubeshackles-ledger`, but is explicitly labeled `SANDBOX_ONLY`; the CIEL signature it attaches is a fabricated string, not a real signature (verified 2026-07-19) | Partially implemented | Verified against repo code and 61 passing tests | State that ledger posting is real and tested, but the signature attached is not cryptographic and the call path is sandbox-only, production would require a signed HTTP call | Direct translation, preserve both halves of the claim | — | 2026-07-19 |
| Settlement receipt hash chain | `cubeshackles-settlement-engine` | `settlement_receipt.py` and `receipt_verifier.py` use real SHA-256 over canonical JSON, independently re-verifiable against live ledger state; hashes cover reference IDs, not full attestation payloads (verified 2026-07-19) | Implemented | Verified against repo code and passing tests | State as genuinely cryptographic and re-verifiable, distinct from the presence-only pattern in `cubeshackles-ledger`; note it hashes IDs, not full payload content | Direct translation | — | 2026-07-19 |
| Settlement-engine event-type-only trust check | `cubeshackles-settlement-engine` | `SettlementInput.is_validated` is true whenever `event_type == "transaction.validated"`, without checking the actual validation decision or a cryptographic attestation signature (verified 2026-07-19) | Partially implemented | Verified against repo code | State explicitly that the trust check is on event type, not decision content or signature | Direct translation | — | 2026-07-19 |
| Institutional gateway compliance/clearing forwarding | `cubeshackles-institutional-gateway` | `interoperability/compliance_gate.py` and `clearing_forwarding.py` exist as tested library functions but are never called from `api/routes.py`; the HTTP API has no authentication of any kind (verified 2026-07-19) | Prototype | Verified against repo code | State explicitly that the "Gateway → Compliance → Clearing" control chain is not enforced by any live code path yet, and the API is unauthenticated | Direct translation, do not imply an enforced pipeline | — | 2026-07-19 |
| Institutional gateway protocol adapters (FIX/ISO20022/SWIFT/gRPC) | `cubeshackles-institutional-gateway` | Adapters map a narrow, simplified field profile with hardcoded fallback defaults (e.g. `institution_id` → `"UNKNOWN"`, settlement date → `"2099-01-01"`, default currency `"USD"` not `"AOA"`); no live counterparty connectivity of any kind (verified 2026-07-19) | Prototype | Verified against repo code | State as protocol-format adapters exercised against synthetic payloads only, not live institutional connectivity | Direct translation | — | 2026-07-19 |
| Regulatory reporting outbound connectivity | `cubeshackles-regulatory-reporting` | Confirmed zero network-client dependencies and zero outbound-connection code anywhere in `src/`/`scripts/`; reads local JSON, writes local files/HTTP response only (verified 2026-07-19, re-confirms 2026-07-11 audit finding) | Prototype | Verified against repo code and dependency manifest | "This repository generates technical evidence artifacts from local input; it has no outbound connection to BNA, CMC, BODIVA, or any regulator system." | Direct translation | — | 2026-07-19 |
| Regulatory reporting Terrain enforcement layer | `cubeshackles-regulatory-reporting` | `reporting_terrain/` (guard, provenance, permission model) is implemented and has its own passing test suite, but is never imported by the live report-generation pipeline (verified 2026-07-19) | Prototype | Verified against repo code | State explicitly that this enforcement layer exists in code but does not govern the live API/script report-generation path | Direct translation, do not imply active enforcement | — | 2026-07-19 |
| Contracts schema-validity test coverage | `cubeshackles-contracts` | `docs/integration-test-strategy.md` claims every schema is valid JSON Schema and every example validates against it; actual `tests/` (3 tests) only check file existence, do not parse/validate schema content (verified 2026-07-19) | Proposed | Verified against repo code — the strategy doc's claim is aspirational, not implemented | State test coverage accurately: file-existence checks only; schema/example validation is proposed, not implemented | Direct translation | — | 2026-07-19 |
| "56 shackle specs / 413 tests" figure | `cubeshackles-contracts` | Exists only on an unmerged branch `feat/sprint1-shackle-specs`, not an ancestor of `main`; absent from the checked-out tree entirely (verified 2026-07-19) | Not independently verified | Verified against repo git history — figure does not describe current `main` | Do not cite this figure for the current state of `cubeshackles-contracts`; if referenced historically, label it explicitly as unmerged/superseded | Direct translation | — | 2026-07-19 |

| DAG primitive integration | `Cubeshackles-core` | `src/cubeshackles/dag/{node,edge,hashing,validation,traversal,state_root}.py` are implemented with deep passing unit coverage, but `core/orchestrator.py` uses an older `DAGState`/`validate`/`rebuild` trio instead — the new primitives are not yet called in the live execution path (verified 2026-07-19) | Implemented (standalone), not integrated | Verified against repo code and 128 passing tests | State both halves: the new primitives are real and tested, but not yet wired into orchestration | Direct translation | — | 2026-07-19 |
| SmartShackle risk evaluation | `Cubeshackles-core` | `smartshackles/policy.py::PolicyEngine.validate()` calls only `tx.validate_basic()`; no risk-scoring logic exists anywhere outside the `contracts/` schema layer, despite the transaction-lifecycle diagram listing it as an "advisory" step (verified 2026-07-19) | Planned | Verified against repo code | State as a contract surface and documented intent, not executed logic | Direct translation | — | 2026-07-19 |
| Validator quorum computation | `Cubeshackles-validator-node` | `validate_attestation_quorum()` (real multi-attestation de-dup/signature-verification/threshold logic) exists and is unit-tested, but has zero callers outside its own test file — the one live HTTP path (`attest_batch_state`) processes a single self-signed attestation and derives `quorum_status` from a caller-supplied value, not this function (verified 2026-07-19). Corrects a 2026-07-11 audit finding ("quorum is a dashboard field only") to be more precise: real quorum logic exists and is tested, it just isn't wired into the only live endpoint | Implemented (standalone), not integrated | Verified against repo code and 98 passing tests | State that real Ed25519 signature verification exists (this repo does NOT have the presence-only signature bug found elsewhere), but multi-validator quorum-gating is not enforced on the live path | Direct translation, preserve the correction to the prior audit framing | — | 2026-07-19 |
| Validator-node fault-injection scripts | `Cubeshackles-validator-node` | `scripts/enable_fake_divergence.sh`/`restore_honest_attestation.sh` monkey-patch source to inject a fake diverging state root for byzantine-fault-simulation demos, then restore via git (verified 2026-07-19) | Experimental | Verified against repo code | Disclose as a deliberate fault-injection test harness, not production code or a vulnerability | Direct translation | — | 2026-07-19 |
| Runtime deterministic execution engine | `cubeshackles-runtime` | `src/execution/engine.py::ExecutionEngine.execute` and the scheduler/state_machine/dag_engine/runtime_kernel modules all raise `NotImplementedError` on main; a separate, tested "gateway handoff" lifecycle (queue→validate→dispatch→finalize state machine) exists alongside them but stops short of executing state transitions (verified 2026-07-19, re-confirms 2026-07-11 audit finding, adds detail) | Planned (execution core); Implemented (handoff/boundary layer) | Verified against repo code and 50 passing tests, none of which touch the execution-core modules | State precisely which layer is which: the marketed "deterministic execution" core is unimplemented; a real, tested boundary/persistence/Terrain-enforcement layer exists around it | Direct translation | — | 2026-07-19 |
| Node-api citizen demo transfer | `Cubeshackles-node-api` | `POST /v0.1/demo/transfer` makes a real outbound HTTP call to a separate settlement-engine service (not mocked), but matches neither of the repo's two auth-middleware path allowlists — it is unauthenticated (verified 2026-07-19) | Partially implemented | Verified against repo code | Public sibling READMEs: state that auth/Terrain coverage is route-scoped and that the demo rail is Partially implemented; point here for tracked gaps. Do **not** publish path-level unprotected-endpoint maps (path + no auth + real settlement) in public README text (CLAUDE.md rule 11). | Same discipline in Portuguese; do not restate the auth gap as an exploit map | — | 2026-07-19; public-wording tightened 2026-07-22 |
| Network-orchestrator coordination model | `Cubeshackles-network-orchestrator` | `src/network/`, `src/validators/`, `src/routing/` (node registry, health view, replay-aware routing) are in-memory dataclasses with zero test coverage and zero callers from `app/main.py` or any script — the only production/tested surface is a Kulifikila integration router and CIEL event forwarding (verified 2026-07-19) | Planned | Verified against repo code — grepped for callers, none found outside the modules' own files | State plainly that "multi-node topology coordination" is unwired scaffolding; only the Kulifikila/CIEL HTTP surface and shell-script stack orchestration are exercised | Direct translation | — | 2026-07-19 |
| Offline-infrastructure sync/queue path | `cubeshackles-offline-infrastructure` | `SyncTransport.transmit()` raises `NotImplementedError` (confirms 2026-07-11 finding); additionally, `OfflineQueueManager.enqueue_initiated()` and `SyncManager.attempt_sync()` both contain an unconditional `raise forbidden_action(...)` on the first loop iteration, making the entire queue-admission path unreachable — confirmed by direct execution (verified 2026-07-19, more severe than the prior finding) | **Severity: P0 implementation blocker** (not ordinary technical debt) — Prototype/non-functional scaffold | Verified against repo code by direct execution; zero tests exist in the repository | "The current queue-admission path raises before accepting or persisting work. Offline transaction processing is therefore not operational in the present implementation." | "O fluxo actual de admissão à fila gera uma excepção antes de aceitar ou persistir qualquer operação. Consequentemente, o processamento de transacções offline não está operacional na implementação actual." | — | 2026-07-19 (founder-reviewed severity language 2026-07-19) |
| Terrain as a callable backend service | `cubeshackles-terrain` | This is the canonical Terrain system other repos' in-memory stub clients (gateway_terrain, reporting_terrain, settlement_terrain, etc.) are meant to eventually call — but it has no HTTP framework, no server, and no CLI anywhere; it is itself only a Python library, same maturity tier as the stubs waiting on it (verified 2026-07-19) | Planned (as a service); Implemented (as a library) | Verified against repo code and 253 passing tests (96.37% coverage) | State that schema/audit/snapshot/event-contract logic is real and well-tested (the "real depth" claim holds there), but there is no network-callable service, no policy/permission engine, no durable state, and no real cryptographic signing (`ciel_bridge.py`'s "signature" is a formatted string) | Direct translation, preserve both halves | — | 2026-07-19 |

| Runtime Terrain enforcement fail-open path | `cubeshackles-runtime` | Cross-repository review (2026-07-19) found `terrain_guard.py::_verify_object`/`_verify_link` and `terrain_context.py::verify_runtime_context` catch `ModuleNotFoundError` and silently return as-if-passed when the sibling `cubeshackles-terrain` repo isn't importable — a documented design choice in-code ("keep runtime deterministic even if sibling isn't installed"), but a genuine fail-open path. Corrects the original Batch 3 README wording, which said this guard "fails closed" without that caveat | Partially implemented (fails closed only when the sibling repo is present) | Verified against repo code (`terrain_context.py`, `terrain_guard.py`) | State explicitly: Terrain object/link/context validation is skipped, not enforced, when the sibling repo is absent — this is fail-open, not fail-closed, distinct from `Cubeshackles-node-api`'s equivalent path which explicitly fails closed | Direct translation, preserve the fail-open/fail-closed distinction | — | 2026-07-19 |
| Node-api / validator-node Terrain client integration pattern | `Cubeshackles-node-api`, `Cubeshackles-validator-node` | Both perform a genuine same-machine sibling import of `cubeshackles-terrain`'s real Python modules (not a stub, not a network call — Terrain itself has no network service, see its own Claims Register entry). node-api's `terrain_context_service.py` explicitly fails closed (raises `terrain_internal(...)`) if the sibling is absent; validator-node's equivalent `terrain_available()` check is defined but never called at the verification call sites, so an absent sibling raises an unhandled `ImportError` there instead of a designed error (verified 2026-07-19) | Implemented (both, when the sibling is present); the failure-mode design differs between the two | Verified against repo code | State the integration is a real sibling-package import, not mocked, and describe each repo's specific behavior when the sibling is absent rather than a single blanket claim | Direct translation | — | 2026-07-19 |

| CubeWallet is three unrelated deployables | `CubeWallet` | `mobile/`, `backend/`, and root `main.py`+`wallet-ui/` are three separately-deployed codebases with no shared runtime; the repo's own `backend/README.md` calls the third "legacy" (verified 2026-07-19) | Not applicable (structural finding, not a maturity claim) | Verified against repo code | State explicitly which deployable each capability claim refers to; never make a blanket "CubeWallet does X" claim | Direct translation | — | 2026-07-19 |
| CubeWallet mobile app backend connectivity | `CubeWallet` | The actively-branded `mobile/` client has 10/10 API adapters as `*.adapter.mock.ts`, zero `fetch`/`axios` calls anywhere in `mobile/src`, and a CI gate (`check_cubewallet_backend_connection.py`) that currently bans real HTTP calls from being wired in (verified 2026-07-19, sharpens 2026-07-11 audit finding "100% mocked" — now repo-precise) | Prototype | Verified against repo code, zero tests exist for `mobile/` | State plainly: the mobile app makes no real backend calls today, and its own CI enforces that | Direct translation | — | 2026-07-19 |
| CubeWallet legacy stack is self-custodial | `CubeWallet` | Root `main.py`'s `Wallet` model mutates balances directly in this repo's own Postgres DB with no downstream settlement dependency required to "complete" a transaction from the user's perspective (verified 2026-07-19) | Implemented (as a local ledger, not connected to platform settlement) | Verified against repo code | State this precisely as self-custodial local ledger behavior, not "custody controls" delegated to the protocol layer as doctrine language elsewhere implies | Direct translation | — | 2026-07-19 |
| CubeWallet KYC/AML checks | `CubeWallet` | Both `mobile/kycStore.ts` and `backend/app/services/kyc.py` hardcode NIF/AGT/CMC compliance checks to unconditionally return "cleared" for any sufficiently long input string; `backend/` persists this fabricated result to a real database (verified 2026-07-19) | Prototype | Verified against repo code | State that the verification logic itself is fabricated even where storage is real; do not imply live AGT/CMC connectivity | Direct translation | — | 2026-07-19 |
| CubeWallet USSD/multi-currency/telecom claims | `CubeWallet` | "National product doctrine" language (8 languages, USSD/NFC/Bluetooth, multi-currency BTC/ETH/XRP/SOL, inheritance workflows, AI fraud detection) has no corresponding wired code anywhere in `mobile/` or `wallet-ui/` — exists only as README/blueprint prose and isolated unwired scripts (verified 2026-07-19) | Planned | Verified against repo code | Do not cite as current capability; label explicitly as roadmap/planned | Direct translation | — | 2026-07-19 |
| CubeWallet legacy identity field (SSN vs NIF) | `CubeWallet` | Root `main.py`'s wallet-creation flow hashes a `ssn` (US Social Security Number) field rather than an Angola NIF/BI identifier — a templated field inconsistent with the platform's Angola-first framing (verified 2026-07-19) | Implemented (but factually inconsistent with stated doctrine — a code-level defect, not a documentation issue) | Verified against repo code | Flag explicitly as a known inconsistency worth fixing in code | Direct translation | — | 2026-07-19 |
| Phone Wedge USSD/telecom bridge | `Cubeshackles-phone-wedge` | `/wallet-rail/ussd` (`app/services/ussd_service.py`) is a JSON HTTP endpoint that mimics a USSD menu session; no SS7, carrier API, or mobile-network-operator integration exists anywhere in the repo (verified 2026-07-19) | Prototype | Verified against repo code | State plainly this is a USSD-*shaped* session simulator, not a real telecom bridge | Direct translation | — | 2026-07-19 |
| Phone Wedge phone "verification" | `Cubeshackles-phone-wedge` | `verify_phone()` (`app/services/phone_verification_service.py`) unconditionally marks any correctly-formatted number "verified" after normalization only; `OperatorRegistry`/`SubscriberValidator` (`app/operator_registry/`) are defined and unit-tested but never called from the live `/integrations/phone/verify` route (verified 2026-07-19) | Prototype | Verified against repo code, dead-code check confirmed via grep across `app/routes/` and `app/services/` | State that no external operator/SIM/identity check occurs; the response's own `LOCAL_VERIFICATION_ONLY` field already discloses this at the API layer | Direct translation | — | 2026-07-19 |
| Phone Wedge transfer route surface | `Cubeshackles-phone-wedge` | A prior README version's "Key API routes" table and "Owns" list described `/transfer/preview`, `/transfer/prepare`, `/transfer/session/{id}`, `/transfer/confirm`, `/transfer/execute` — none exist in the codebase; the only real transfer route is the single-shot `POST /wallet-rail/transfer` (verified 2026-07-19) | Not applicable (documentation-code mismatch, not a maturity claim) | Verified against repo code (`app/routes/transfer.py`, `app/routes/wallet_rail.py`) | Document only routes that exist in code; do not imply a preview/confirm UX flow where none is wired | Direct translation | — | 2026-07-19 |
| Phone Wedge offline queue durability | `Cubeshackles-phone-wedge` | `offline_queue_service.py`'s `_pending` list is in-process memory only, with no disk/DB persistence, and does not import the sibling `cubeshackles-offline-infrastructure` package anywhere in the repo (verified 2026-07-19) | Prototype | Verified against repo code | State explicitly that queued records do not survive a process restart and are unrelated to the sibling offline-infrastructure package | Direct translation | — | 2026-07-19 |
| Phone Wedge audit log PII | `Cubeshackles-phone-wedge` | `audit/logger.py::emit_audit_event` persists the caller-supplied `payload` — including plaintext normalized phone numbers — to an on-disk JSONL file with no hashing or redaction, for `phone.verified`, `user.created`, and `identity.linked` events (verified 2026-07-19) | Implemented (as a privacy gap, not a feature) | Verified against repo code | Flag explicitly as a privacy/sensitive-data handling gap for founder/legal review | Direct translation | — | 2026-07-19 |
| Web Explorer/Adviser data sourcing | `Cubeshackles-web` | `components/explorer/` and `app/adviser/` contain zero `fetch()` calls anywhere in the tree; `control-room.tsx` explicitly labels its own data `"All identifiers are fixture-derived in this environment."` — the prior README claim "Every number displayed is sourced from node-api" did not hold for these two views (verified 2026-07-19) | Prototype | Verified against repo code | State per-view which data is live vs. fixture; do not make a blanket "every number is sourced from node-api" claim | Direct translation | — | 2026-07-19 |
| Web Execute flow write authority | `Cubeshackles-web` | `components/execute/transfer-form.tsx` performs a real, single-click, HMAC-signed write to node-api's `/batches/execute` via the `/api/execute` BFF route, with no preview/confirm step and hardcoded sender/receiver accounts (`commit_alice`/`commit_bob`) (verified 2026-07-19) | Partially implemented | Verified against repo code | State that Execute is a real signed write, not a preview-only display; do not claim a confirmation step that does not exist | Direct translation | — | 2026-07-19 |
| Web operator authentication model | `Cubeshackles-web` | `lib/operator-session.ts` implements a single shared token (not per-operator identity), compared with a non-constant-time `!==`, defaulting to the hardcoded literal `"operator-session-local"` if `CUBESHACKLES_OPERATOR_SESSION_TOKEN` is unset; no CSRF protection or Content-Security-Policy header exists anywhere in the codebase (verified 2026-07-19) | Prototype | Verified against repo code | Flag explicitly as a security gap for founder/legal review before this console is exposed beyond trusted internal operators | Direct translation | — | 2026-07-19 |
| Web Supervision/Kulifikila regulatory access | `Cubeshackles-web` | `app/supervision/` and `app/kulifikila/` are real node-api proxies (genuine reads, not fixture data), but any holder of the single shared operator token can view them under the `node_api_admin` role — there is no regulator-specific authentication model (verified 2026-07-19) | Prototype | Verified against repo code | State that these views are real but access-controlled only by the shared operator token, not by a regulator-specific identity check | Direct translation | — | 2026-07-19 |
| National Transit App settlement claims | `national-transit-app-cubeshackles` | No validator, settlement-engine, or ledger call exists anywhere in the codebase; the documented "AOA settlement flow" is an architecture diagram, not a wired path. The two FastAPI apps in the repo are disconnected — the frontend's driver payment button calls `/process-payment/`, a route that exists only in a separate app the deployed backend never mounts (verified 2026-07-19) | Planned | Verified against repo code, confirmed by running the actual test suite (9 passed, none exercise payment/settlement logic) | State explicitly that no fare payment is currently settled through the CubeShackles protocol; do not describe the settlement flow diagram as a current capability | Direct translation | — | 2026-07-19 |
| National Transit App fare/fee data | `national-transit-app-cubeshackles` | Balance is a hardcoded literal regardless of user; two different hardcoded fee formulas exist (0.29%+1% vs 0.29%+1%+0.5%), neither sourced from any pricing engine despite `contracts/CONTRACTS.md` claiming one exists (verified 2026-07-19) | Prototype | Verified against repo code | State that fee calculation is two hardcoded formulas, not a pricing engine; do not imply a single authoritative fee source | Direct translation | — | 2026-07-19 |
| National Transit App authentication | `national-transit-app-cubeshackles` | No endpoint in the repository checks any credential, role, or identity; the documented driver/passenger/operator access-tier separation is a design intent, not an enforced boundary (verified 2026-07-19) | Planned | Verified against repo code | Flag explicitly as a security gap for founder/legal review before any real fare data is wired in | Direct translation | — | 2026-07-19 |
| BualaBuitu economic indicator data | `BualaBuitu` | `indicator_service.py::get_angola_indicators()` and `get_province_indicators()` are unconditional hardcoded literals, each explicitly labeled `"(placeholder)"` in code — unlike FX/market data, which attempt a real call to node-api first (verified 2026-07-19) | Prototype | Verified against repo code | State that indicator data never attempts a live source, unlike FX/market data which does; do not group them together as equally "live-with-fallback" | Direct translation | — | 2026-07-19 |
| BualaBuitu production demo-data guard | `BualaBuitu` | `production_guard.py::reject_demo_only_pull()` exists to forbid demo-only data in production mode, but is called only from a unit test — not wired into `fx_service.py`, `market_ingestion.py`, or `indicator_service.py` (verified 2026-07-19) | Prototype | Verified against repo code | Flag explicitly that this guard is not currently enforced on any live data endpoint | Direct translation | — | 2026-07-19 |
| BualaBuitu export signing | `BualaBuitu` | Both signing providers (`LOCAL_DEV_SHA256`, `EXTERNAL_HSM_STUB`) compute SHA-256 checksums, not asymmetric cryptographic signatures; `EXTERNAL_HSM_STUB`'s own code comment states it makes no network calls to any external HSM (verified 2026-07-19) | Prototype | Verified against repo code | Do not represent "signed export audit" as tamper-evident against a party who can compute SHA-256; real HSM integration is tracked as roadmap in `docs/ROADMAP.md` | Direct translation | — | 2026-07-19 |
| BualaBuitu read-only execution boundary | `BualaBuitu` | `CubeShacklesAdapter.READ_ONLY_METHODS` and `assert_read_only_method()` enforce GET-only calls toward node-api in code, not just doctrine; the one write path is HMAC-signed and gated by a node-api-availability check (verified 2026-07-19) | Implemented | Verified against repo code | This is a positive finding — state plainly that the read-only claim is code-enforced, not just documented | Direct translation | — | 2026-07-19 |
| Adviser non-execution boundary | `cubeshackles-adviser` | No code path posts a trade, settles, or commits a ledger entry — enforced at multiple layers (PolicyMiddleware blocked-intent check, FORBIDDEN_ADVISER_USE_CASES allowlist, disabled-by-default ledger client that only ever proposes, never posts, a journal entry) (verified 2026-07-19) | Implemented | Verified against repo code, backed by `test_no_auto_execution.py`/`test_policy_block.py` | This is a positive finding — state plainly that the non-execution boundary is code-enforced across multiple independent layers, not just documented doctrine | Direct translation | — | 2026-07-19 |
| Adviser five core agents' AI/ML status | `cubeshackles-adviser` | The five core agents (PortfolioAI/RebalanceAI/TaxAI/AlertAI/SimulationAI) call an LLM but discard the result — `apps/adviser-api/app/services/orchestrator.py` awaits `self.llm.complete_json(...)` without assigning the return value; responses are built by deterministic Python logic instead. A version-registry label of `"ml_v1"` could misleadingly imply a trained model is in use (verified 2026-07-19) | Prototype | Verified against repo code | State explicitly that no live LLM/ML output drives these five agents' actual recommendations, despite version labels that could suggest otherwise; do not describe them as AI-native without this qualifier | Direct translation | — | 2026-07-19 |
| Adviser dual-codebase architecture | `cubeshackles-adviser` | The repository contains two parallel FastAPI codebases — a smaller root `app/` and the actually-deployed `apps/adviser-api/app/` (`make run` boots the latter via `--app-dir apps/adviser-api`) — the prior README's Architecture section documented only the root tree (verified 2026-07-19) | Not applicable (structural finding, not a maturity claim) | Verified against repo code | Document both codebases and state clearly which one is actually deployed; do not describe only the smaller/older tree | Direct translation | — | 2026-07-19 |
| Adviser wallet-consent and CIEL-signature stubs | `cubeshackles-adviser` | `wallet_auth.py::verify_consent()` treats any non-empty signature string as verified with no cryptographic check; `ledger_client.py::_ciel_headers()` defaults to a literal stub value if the caller omits a signature rather than rejecting the request (verified 2026-07-19) | Prototype | Verified against repo code | Public sibling READMEs: state that wallet-consent / CIEL-header paths are non-cryptographic Prototype stubs and point here for tracked gaps. Do **not** publish path-level bypass maps (e.g. any non-empty string verifies) in public README text (CLAUDE.md rule 11). Flag as a security gap for founder/legal review before real funds or real CIEL authority | Same discipline in Portuguese; do not restate stub bypass as an exploit map | — | 2026-07-19; public-wording tightened 2026-07-22 |

## How to add a row

1. Identify the claim in an English README/doc.
2. Cite the specific code/test/doc that supports (or fails to support) it.
3. Assign a maturity label from `LOCALIZATION_POLICY.md` §4.
4. Draft approved EN/PT wording that matches the evidence, not the aspiration.
5. Leave Reviewer/Last reviewed blank until a human (ideally the founder for
   institutional/regulatory claims) signs off.

## Related

- [`DOCUMENTATION_STANDARD.md`](DOCUMENTATION_STANDARD.md)
- [`LOCALIZATION_POLICY.md`](LOCALIZATION_POLICY.md)
